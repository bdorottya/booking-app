import { SelectChangeDirective } from './select-change.directive';

describe('SelectChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectChangeDirective();
    expect(directive).toBeTruthy();
  });
});
